spring:
  application:
    name: demo

server:
  port: 9090

eureka:
  instance:
    instance-id: "${spring.application.name}:${random.value}"
  client:
    service-url:
      defaultZone: http://127.0.0.1:8761/eureka/
